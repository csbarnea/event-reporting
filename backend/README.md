POST /api/incidents

GET /api/incidents

GET /api/incidents/<id>


add_incidents_db.py - adauga incidents in db 
python add_incidents_db.py
curl http://localhost:5000/api/incidents to view incidents



1. porneste db din docker compose din folder backend
2. porneste backend = python app.py
3. porneste frontend = pnpm dev